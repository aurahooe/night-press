# The Night Press

An hourly public desk. Sign in, file a note, mark it public, and it appears on the board. Every hour `/api/tick` chooses a featured piece.

Set `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`. Optional `CRON_SECRET` protects the ticker.
